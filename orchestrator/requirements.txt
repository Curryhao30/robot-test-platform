# P0 运行时依赖（Python 3.11）
grpcio==1.71.0
grpcio-tools==1.71.0
protobuf>=3.20.3,<6.0.0dev
pytest>=8.0
PyYAML>=6.0
numpy>=1.26
fastapi>=0.110
uvicorn>=0.27
# starlette.testclient 新版要求 httpx2（CI 装最新 starlette 时必需；旧版使用 httpx）
httpx2
playwright>=1.45
# 桌面示教器面板（PyQt5 HMI，补齐 QT 缺口；teach_pendant/qt_panel.py）
PyQt5>=5.15
PyQtWebEngine>=5.15
